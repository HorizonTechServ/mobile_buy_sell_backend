server:
  port: ${ENV_SERVER_PORT:9090}
  servlet:
    context-path: ${ENV_CONTEXT_PATH:/society/api}

  error:
    whitelabel:
      enabled: false
    path: /error-413

logging:
  print:
    payload: ${ENV_PRINT_PAYLOAD:true}
  level:
    root: ${ENV_ROOT_LOG_LEVEL:INFO}
    com.one: ${ENV_ONE_LOG_LEVEL:DEBUG}

firebase:
  config:
    path: config/firebase/firebase-service-account.json

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

  jpa:
    show-sql: true # Enables SQL query logging
    properties:
      hibernate:
        jdbc.batch_size: 30
    hibernate:
      naming:
        physical-strategy: com.one.societyAPI.config.CustomCaseNamingStrategy
      ddl-auto: update # Use 'create' for first-time setup, 'update' for changes
  jackson:
    serialization:
      write-dates-as-timestamps: false
  profiles:
    active: ${ENV_PROFILE:local}
  output: 
    ansi: 
      enabled: always
  application:
    name: ${ENV_APP_NAME:society_api}
  datasource:

    #url: jdbc:mysql://13.233.142.6:3306/society
    #username: m00nSocietry
    #password: m00nSocietry


    url: jdbc:mysql://localhost:3306/society
    username: root
    password: root

    #url: ${ENV_DB_URL:jdbc:mysql://13.233.142.6:3306/society}
    #username: ${ENV_DB_USERNAME:m00nSocietry}
    #password: ${ENV_DB_PASSWORD:m00nSocietry}
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        database-platform: org.hibernate.dialect.MySQL8Dialect
        hibernate:
          ddl-auto: update  # Change as needed: none, validate, create, or create-drop
        show-sql: true


  mail:
    host: smtp.gmail.com
    port: 587
    username: Motionsorange@gmail.com
    password: qfei kzld eezj xuuz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
cors:
  allowed-origins: "http://localhost:3000"


springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

jwt:
  secret: "c2VjdXJlLXNlY3JldC1rZXktZm9yLWp3dC1hdXRoZW50aWNhdGlvbg=="  # Replace with a strong secret key
  expirationMs: 7200000       # Token expiration time in milliseconds (e.g., 2 hour)
  refreshExpirationMs: 604800000 # 7 days validity for refresh token (604800000)

management:
  endpoints:
    web:
      exposure:
        include: "*" 


feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 5000